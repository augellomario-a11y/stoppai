version: "3.8"
services:
  stoppai-backend:
    build: .
    container_name: stoppai-backend
    restart: unless-stopped
    ports:
      - "127.0.0.1:6002:6002"
    env_file:
      - .env
    volumes:
      - ./db:/app/db
